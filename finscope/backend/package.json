{
  "name": "finscope-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Convenience meta-package for running FinScope backends",
  "scripts": {
    "start:node": "cd node && npm start",
    "start:python": "cd python && uvicorn main:app --reload --port 8000"
  }
}
